<h1>Add New</h1>

<%= form_for @project do |f| %>

    <p>
	<%= f.label :title %><br>
	<%= f.text_field :title %>
	<% if @project.errors.any? #空のデータ保存?%>
	    <%= @project.errors.messages[:title][0] %>
	<% end %>
    </p>

    <p>
	<%= f.submit %>
    </p>

<% end %>
